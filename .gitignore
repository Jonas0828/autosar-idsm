# Build output
build/
Testing/

# Third-party source trees (not committed; fetch via download, see
# third_party/suricata/fetch.sh). Release tarballs ARE committed so
# offline / slow-network build servers can build without re-downloading.
third_party/suricata/suricata-*/

# IDE / OS noise
.vs/
.vscode/
*.user
Thumbs.db

# host_probe: machine-local generated baselines (regenerate per host)
/files.txt
/mods.txt

# tool / agent noise
.claude/
NUL/
__pycache__/
