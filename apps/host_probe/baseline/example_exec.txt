# host_probe exec allowlist -- one entry per line:
#   exact exe path (contains '/')  ->  /usr/bin/ls
#   bare basename                  ->  sshd
# Anything not listed alerts as DT_UNKNOWN_EXEC (unknown program exec)
# once the first entry is loaded. Generate a starting point from a
# quiet system, e.g.:  ps -eo comm= | sort -u >> allowlist.txt

# --- generic Linux daemons / tools ---
/usr/lib/systemd/systemd
/usr/sbin/sshd
/usr/sbin/cron
/usr/bin/dbus-daemon
sshd
bash
sh
ls
ps

# --- Android (basenames; exe paths live under /system, /vendor, /apex) ---
init
ueventd
logd
servicemanager
hwservicemanager
vold
netd
adbd
zygote
zygote64
system_server
surfaceflinger
audioserver
cameraserver
mediaserver
installd
keystore
gatekeeperd
debuggerd
